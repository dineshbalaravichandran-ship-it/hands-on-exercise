# Exercise 7 - shoppingapp

## Objective
Create a react app "shoppingapp" with class components `Cart`
(properties: itemname, price) and `OnlineShopping`, which holds an
array of 5 `Cart` items in state and lists them.

## How to run
```
npm install
npm start
```
