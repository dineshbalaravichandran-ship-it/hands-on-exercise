# Exercise 3 - scorecalculatorapp

## Objective
Create a react app "scorecalculatorapp" with a function component
"CalculateScore" that accepts Name, School, Total and Goal as props,
calculates the average score and displays it. Styles are applied using
a separate stylesheet.

## How to run
```
npm install
npm start
```
